{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "outDir": "../out-tsc/app",
    "types": [
    	"gapi",
    	"gapi.auth2",
      "../types/dictionary"
    ]
  },
  "exclude": [
    "test.ts",
    "**/*.spec.ts"
  ]
}
